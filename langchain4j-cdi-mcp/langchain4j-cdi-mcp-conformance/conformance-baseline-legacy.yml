# Known failures of langchain4j-cdi-mcp against @modelcontextprotocol/conformance
# for the legacy (stateful) runs:
#
#   npx -y @modelcontextprotocol/conformance@alpha server \
#       --url http://localhost:8080/mcp --spec-version 2025-11-25 --suite all \
#       --expected-failures conformance-baseline-legacy.yml
#   npx -y @modelcontextprotocol/conformance@0.1.16 server \
#       --url http://localhost:8080/mcp \
#       --expected-failures conformance-baseline-legacy.yml
#
# Note: the legacy era implemented by this server is MCP 2025-03-26, while these
# scenarios target 2025-06-18 / 2025-11-25. Not one failure is attributable to
# that difference.
#
# Scores at the time of writing: 2025-11-25 -> 74 passed, 1 failed;
# stable line @0.1.16 -> 40 passed, 0 failed (a clean sweep).
server:
  # No API to supply a hand-written tool inputSchema: the schema is always
  # generated from the Java signature, so the SEP-1613/SEP-2106 fixture tool
  # `json_schema_2020_12_tool` cannot be expressed. The scenario's second check
  # passes and stays enforced; the scenario is absent from the @0.1.16 suite.
  - json-schema-2020-12:json-schema-2020-12-tool-found
